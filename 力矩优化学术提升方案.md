# RL_BOY 力矩优化学术提升方案

## 1. 文档目的

本文给出 mjlab 中 RL_BOY 平地 velocity 任务的力矩优化方案。目标不是单纯
增加一个力矩惩罚项，而是在保证起身、抗扰和速度跟踪能力的前提下，降低：

- 限幅前不可实现的力矩请求；
- 峰值力矩使用率和持续时间；
- 超过连续额定力矩的累计负载；
- 力矩高频变化和冲击；
- 机械功率与单位距离能耗。

方案基于现有起身—行走一体化任务和
`DYNAMICS_HISTORY_ENCODER_IMPLEMENTATION_PLAN.md`，建议形成独立任务：

```text
Mjlab-Velocity-Flat-RL_BOY-RAAB-DynHist
```

其中 RAAB 表示：

```text
Recovery-Aware Actuator Budgeting
```

原有任务必须保持可训练和 checkpoint 兼容：

```text
Mjlab-Velocity-Flat-RL_BOY
Mjlab-Velocity-Flat-RL_BOY-GRU
Mjlab-Velocity-Flat-RL_BOY-DynHist
```

## 2. 总体结论

推荐的论文方法不是“用 Transformer 降低力矩”，而是：

> 面向起身—行走连续控制中执行器需求随恢复阶段剧烈变化的问题，使用
> requested-applied torque discrepancy 表征不可实现的控制请求，联合短时
> 动力学历史和长时执行器负载状态，并通过恢复阶段条件的显式力矩预算进行
> 约束优化。

完整方法包含三个不可缺少的核心组件：

1. **执行器差值历史编码**：同时编码 requested torque、applied torque 及二者
   差值，识别限幅、负载变化和执行器能力不足。
2. **双时间尺度力矩状态**：0.2 秒神经历史负责瞬态动力学，0.5 秒和 2 秒
   I²t/EMA 状态负责持续负载。
3. **阶段条件 Primal-Dual PPO**：起身阶段允许必要的短时峰值，行走阶段采用
   更严格预算，持续负载约束在所有阶段保持有效。

辅助预测下一周期执行器响应可以作为论文完整版的第四项，但不是首个工程版本的
前置条件。

## 3. 学术定位与创新边界

下列单独元素不宜声称为本工作的核心创新：

- 历史 observation stacking；
- Transformer/GRU 时序编码；
- 从历史推断隐式动力学；
- torque squared 或 power reward；
- 电机温度或 I²t 状态；
- 普通 PPO-Lagrangian；
- 起身辅助课程。

这些方向已有相关工作。例如：

- RMA 使用本体历史进行快速动力学适应；
- RGMT 使用因果历史编码器和倒地恢复课程；
- VPIES 使用双时间尺度本体历史；
- ECO 将能耗目标改写为显式约束；
- thermal-aware locomotion 将电机热状态加入策略；
- actuator-constrained locomotion 显式考虑 torque-speed operating region。

本文建议的创新组合应聚焦于：

1. **统一起身—行走策略中的阶段相关力矩预算**  
   起身和稳定行走具有冲突的最优执行器使用方式。静态 penalty 不是合适的统一
   目标，本方案为不同恢复阶段定义不同的峰值预算。

2. **requested-applied discrepancy 作为在线执行器可实现性表征**  
   不只惩罚实际输出，而是利用限幅前请求、实际输出和 torque-speed envelope
   之间的差异，帮助策略识别不可实现控制。

3. **瞬态动力学和累计执行器负载的联合建模**  
   神经历史窗口处理冲击、接触和控制响应，解析式慢状态处理持续力矩。两者具有
   不同物理含义，不依赖用超长 Transformer 窗口近似热积分。

4. **恢复能力—执行器安全的可解释 Pareto 分析**  
   论文结论应建立在零辅助起身成功率、任务恢复时间、力矩预算违反率和能耗的
   Pareto 曲线上，而不是单一 episode reward。

在完成正式 related-work 检索前，不应使用“首次”表述。推荐使用：

> We introduce a recovery-aware actuator budgeting framework...

而不是：

> We are the first to use torque history or thermal states...

## 4. 当前实现的问题

### 4.1 当前峰值惩罚没有约束限幅前请求

当前 `actuator_torque_limit_excess_penalty` 读取：

```python
self._asset.data.actuator_force
```

这是限幅后的实际 actuator force。策略请求 25 Nm 和请求 100 Nm，在二者最终
都被裁剪到 20 Nm 时，现有峰值 penalty 几乎无法区分。

虽然 actor 已接收 `requested_torque_peak_ratio`，但 reward 没有直接约束该量。
因此策略缺少“不要提出不可实现请求”的直接学习信号。

### 4.2 当前连续力矩是瞬时约束

现有连续力矩成本为：

```text
relu(abs(applied_torque) / continuous_limit - 1)²
```

它把短时超过连续额定力矩和长时间持续超限同等处理。对起身任务而言，短时使用
峰值力矩可能是合理且必要的；真正需要限制的是累计热负载和超限持续时间。

### 4.3 当前 peak penalty 的有效梯度较弱

现有 peak threshold 为峰值的 0.85，输入又是已经裁剪的 actual torque，因此每个
关节最大 excess 仅为：

```text
1.0 - 0.85 = 0.15
```

平方后最大仅为 0.0225。与速度跟踪、姿态和恢复奖励相比，该项很容易过弱。

### 4.4 力矩权重在课程后期阶跃启用

当前 torque penalty 在恢复课程达到指定 level 前权重为零，之后直接切换成固定
权重。该方式可能产生：

- 策略早期形成依赖饱和力矩的局部最优；
- 权重开启时性能突降；
- 恢复环境和正常行走环境同时受到相同约束；
- 无法根据实际违反率自动调整约束强度。

### 4.5 actuator 模型允许峰值持续输出

RL_BOY 当前 `DcMotorActuatorCfg` 的 `effort_limit` 与
`saturation_effort` 均采用峰值能力：

```text
arm:        3 Nm
leg:       20 Nm
waist/foot: 11 Nm
```

项目中定义的 0.8/8/4 Nm continuous limits 目前主要用于 observation、metric
和 reward，而不是 actuator 内部的热保护。

首版不要把 actuator `effort_limit` 直接改成 continuous limit，否则可能使起身
物理上不可行。应保留瞬时峰值能力，通过累计状态和约束学习其合理使用方式。

## 5. 方法总览

训练和部署数据流如下：

```text
                physics/control feedback
                          │
                          ▼
              ActuatorStressTracker
          (reward 后、自动 reset 前更新)
                ├─ requested torque
                ├─ applied torque
                ├─ requested-applied gap
                ├─ 0.5 s stress EMA
                └─ 2.0 s stress EMA
                          │
            ┌─────────────┴─────────────┐
            ▼                           ▼
  actor deployable observations    extras["torque_costs"]
            │                           │
            ▼                           ▼
  10-frame history encoder       phase-conditioned budgets
            │                           │
            └─────────────┬─────────────┘
                          ▼
                 Primal-Dual PPO
                          │
                          ▼
                    20-D action
```

部署策略不接收训练期 cost、恢复辅助状态或仿真特权 phase label。actor 只使用
实机可构造的本体观测、控制反馈和慢力矩状态。

## 6. 执行器信号定义

所有力矩量按照 actuator 顺序排列，维数均为 20。

tracker 必须直接读取未经过 observation `clip` 的原始 torque。当前 actor 的
continuous ratio 会裁剪到 `[-2, 2]`，但腿部峰值与 continuous limit 的比例可达
`20 / 8 = 2.5`；如果用裁剪后的 observation 更新 EMA，会系统性低估高负载。

建议把 `_CONTINUOUS_TORQUE_LIMIT_BY_ACTUATOR` 和
`_PEAK_TORQUE_LIMIT_BY_ACTUATOR` 从 `env_cfgs.py` 移到
`actuator_stress.py` 或独立 constants 模块，observation、cost、metric、metadata
和部署代码统一引用，避免限制值出现多份副本。

### 6.1 限幅前请求力矩

```text
tau_requested
```

数据来源：

```python
IdealPdActuator.computed_effort
```

它表示 PD 和电机模型裁剪前的控制请求。

峰值归一化：

```text
r_requested_peak =
  tau_requested / peak_torque_limit
```

### 6.2 实际执行力矩

```text
tau_applied
```

数据来源：

```python
entity.data.actuator_force
```

连续额定归一化：

```text
r_applied_cont =
  tau_applied / continuous_torque_limit
```

峰值归一化可以由 continuous ratio 与固定限制比例推导，不需要作为独立 actor
输入。

### 6.3 请求—执行差值

定义：

```text
r_gap =
  (tau_requested - tau_applied) / peak_torque_limit
```

建议 clip 到：

```text
[-2.0, 2.0]
```

该量包含：

- 固定峰值裁剪；
- torque-speed envelope 裁剪；
- 请求方向与实际输出不一致；
- 动作、PD 状态与执行器能力之间的失配。

### 6.4 力矩变化率

定义控制周期级离散变化：

```text
r_delta_tau =
  (tau_applied[t] - tau_applied[t - 1]) / peak_torque_limit
```

首版以 50 Hz 控制周期计算。若后续发现 physics substep 内存在被漏掉的尖峰，再
扩展成四个 substep 的 RMS/max accumulator。

## 7. 双时间尺度持续负载状态

### 7.1 EMA 定义

对每个 actuator 维护：

```text
u[t] = abs(tau_applied[t]) / continuous_torque_limit

h_T[t] =
  alpha_T * h_T[t - 1]
  + (1 - alpha_T) * square(u[t])

alpha_T = exp(-control_dt / T)
```

首版采用：

```text
control_dt = 0.02 s
T_short    = 0.5 s
T_long     = 2.0 s
```

对应：

```text
h_short: [B, 20]
h_long:  [B, 20]
```

`h=1` 表示在对应时间尺度上约等于持续使用 continuous torque。

### 7.2 reset 语义

环境 reset 后：

```text
h_short = 0
h_long = 0
previous_applied_torque = 0
```

不应像 observation history 一样回填第一帧，因为热负载代表 episode 开始时的
假定电机状态。

实机部署时有两种模式：

1. 冷启动：EMA 清零；
2. 连续运行：切换策略时保留 EMA，不因策略 reset 清零。

导出 metadata 必须声明采用哪种模式。

### 7.3 后续热模型升级

如果能获得电机电阻、热容、热阻和温度传感器数据，可将 EMA 替换为：

```text
C_th * dT/dt = I²R - (T - T_ambient) / R_th
tau = K_t * I
```

首版不依赖这些难以准确获取的参数。EMA/I²t 是工程可实现的代理变量，但论文
中必须明确它不是精确温度预测。

在声称“热安全提升”前，还必须用电机手册或台架数据校准 continuous limit 和
时间常数。未完成标定时，论文指标应称为 `actuator stress proxy` 或
`continuous-torque load`，不能直接称为预测电机温度。

## 8. Actor 观测与网络

### 8.1 快速历史帧

建议每帧使用：

| 顺序 | term | 维数 |
|---:|---|---:|
| 1 | `base_ang_vel` | 3 |
| 2 | `projected_gravity` | 3 |
| 3 | `joint_pos` | 20 |
| 4 | `joint_vel` | 20 |
| 5 | `actions` | 20 |
| 6 | `applied_torque_continuous_ratio` | 20 |
| 7 | `requested_torque_peak_ratio` | 20 |
| 8 | `torque_clip_gap_ratio` | 20 |
|  | 合计 | 126 |

history shape：

```text
actor_history: [B, 10, 126]
```

十帧覆盖 0.2 秒，顺序保持 oldest-to-newest。

### 8.2 当前命令

```text
actor_command: [B, 3]
```

命令不重复写入历史。

### 8.3 慢力矩状态

```text
actor_torque_state: [B, 40]

[h_short, h_long]
```

建议 clip：

```text
[0.0, 9.0]
```

然后使用独立 `EmpiricalNormalization`。

### 8.4 网络融合

```text
history frame MLP:
  126 -> 256 -> 128

causal Transformer:
  token_dim = 128
  heads = 4
  layers = 1
  ffn_dim = 256

history pooling:
  首选 latest token
  对照 max pooling

fusion:
  latest normalized frame  126
  command                    3
  torque state              40
  dynamics embedding       128
  total                    297

actor head:
  297 -> 512 -> 256 -> 128 -> 20
```

对于固定过去窗口，causal mask 不是防止动作使用未来信息所必需的。保留 causal
版本是为了延续原 DynHist 方案，但必须将 `latest token`、`max pooling` 和
bidirectional attention 纳入消融。

### 8.5 critic

critic 继续使用原 flat 任务的特权 observation，不使用 Transformer。可以额外
接收真实 phase、接触和当前 torque cost，但首版建议保持不变，以隔离 actor 和
约束算法的贡献。

## 9. 阶段条件力矩成本

### 9.1 阶段 mask

训练期定义：

```text
recovery_mask =
  current_posture.needs_recovery
  or recovery_assist.assist_active

walking_mask = not recovery_mask
```

其中 `current_posture.needs_recovery` 由 tracker 在 reset 前重新估计，不能只复用
上一控制周期保存的 `fallen_detected`。mask 只用于训练 cost 和日志，不进入
actor。

对于 play 模式没有 `RlBoyRecoveryAssist` 的情况，使用 deployable posture
estimator 生成评估 mask，或全部视为 walking。两类口径不得混在同一统计表中。

### 9.2 请求峰值成本

每关节：

```text
c_requested_i(rho) =
  square(relu(abs(r_requested_peak_i) - rho))
```

阶段阈值：

```text
rho_walking  = 0.80
rho_recovery = 1.00
```

每环境成本：

```text
c_requested = mean_i(c_requested_i)
```

起身允许使用峰值，但仍惩罚超过真实峰值的限幅前请求。

### 9.3 裁剪差值成本

```text
c_gap = mean_i(square(r_gap_i))
```

为了避免数值异常：

```text
r_gap = clip(r_gap, -2.0, 2.0)
```

### 9.4 持续负载成本

```text
c_stress =
  mean_i(square(relu(h_long_i - 1.0)))
```

可增加短时项：

```text
c_short_stress =
  mean_i(square(relu(h_short_i - 1.5)))
```

首版只约束 long stress，short stress 作为 metric，避免约束过多。

### 9.5 力矩变化率成本

```text
c_torque_rate =
  mean_i(square(r_delta_tau_i))
```

恢复阶段不完全禁用，但采用更宽松预算。

### 9.6 聚合成四个约束

首版推荐四个 dual constraints：

```text
C_walk_feasibility =
  E[c_requested(0.80) + 0.5 * c_gap | walking]

C_recovery_feasibility =
  E[c_requested(1.00) + 0.25 * c_gap | recovery]

C_stress =
  E[c_stress]

C_walk_smoothness =
  E[c_torque_rate | walking]
```

条件均值必须按 mask 内有效样本数量归一化：

```text
C_phase =
  sum(mask * cost) / max(sum(mask), 1)
```

如果一个 rollout 内某阶段没有样本，不更新对应 multiplier。

### 9.7 初始预算

在没有基线统计前，不建议将下列数值作为最终预算。可以用作 smoke training
初值：

```text
walk_feasibility_budget     = 0.010
recovery_feasibility_budget = 0.030
stress_budget               = 0.010
walk_smoothness_budget      = 0.020
```

正式实验预算应在 M0 基线评估后确定：

- 行走 feasibility 比基线降低至少 30%；
- recovery feasibility 比基线降低至少 15%；
- stress violation time 比基线降低至少 30%；
- 起身成功率下降不超过 3 个百分点。

一旦确定，所有模型和 seeds 使用相同预算。

## 10. Primal-Dual PPO

### 10.1 优化目标

任务目标：

```text
maximize J_task
```

约束：

```text
C_k <= budget_k
```

增广 reward：

```text
r_aug =
  r_task
  - sum(lambda_k * c_k)
```

dual update：

```text
lambda_k =
  clamp(
    lambda_k + dual_lr_k * (C_k - budget_k),
    min=0,
    max=lambda_max,
  )
```

推荐首版参数：

```text
dual_lr     = 1.0e-2
lambda_init = 0.0
lambda_max  = 10.0
warmup_iterations = 200
update_interval   = 1 PPO iteration
```

warmup 期间只统计 cost，不更新 lambda。之后前 200 个 iteration 将 dual update
线性放大到完整 `dual_lr`，避免策略突然失去起身能力。

### 10.2 与 RSL-RL 5.2 的低侵入接入

不要修改 `.venv` 中的 rsl_rl 源码。新增：

```text
src/mjlab/rl/constrained_ppo.py
```

主类：

```text
RecoveryAwareConstrainedPPO
```

继承：

```python
from rsl_rl.algorithms import PPO
```

首版覆盖：

- `__init__()`：创建 budgets、lambda 和 rollout cost accumulator；
- `process_env_step()`：从 extras 读取当前 action 导致的 cost，
  构造 augmented reward，再调用父类；
- `update()`：调用 PPO update，执行 dual ascent，并加入 loss/log 字段；
- `save()`：保存 multipliers、budgets 和 dual optimizer state；
- `load()`：恢复约束状态。

actor 和 critic 的 PPO loss 不需要第一版就增加 cost critic。该实现等价于在每个
rollout 中固定 lambda，并在 rollout 间更新对偶变量。

论文完整版可以增加独立 cost critic 和 cost advantage，但必须作为单独版本，
避免首版工程范围失控。

### 10.3 cost 数据通道

不要将 cost 作为 post-reset observation 传递。当前环境在 reward 和
`MetricsManager.compute()` 之后执行自动 reset，然后才计算新 observation。如果
cost 放在新 observation 中，发生 reset 的环境会丢失终止步 cost。

建议由 tracker 在 reset 前写入：

```text
extras["torque_costs"]: [B, K]
```

包含：

```text
walk_feasibility
recovery_feasibility
stress
walk_smoothness
walking_mask
recovery_mask
```

Tensor 必须使用 `clone()` 写入 extras，避免随后 partial reset 清理 tracker 状态
时修改已经提交的终止步 cost。

runner 的 obs groups 是：

```python
{
  "actor": (
    "actor_history",
    "actor_command",
    "actor_torque_state",
  ),
  "critic": ("critic",),
}
```

actor 的慢力矩状态仍通过 observation group 输入；训练 cost 不属于 observation。
自定义 PPO 在 `process_env_step()` 中读取：

```python
extras["torque_costs"]
```

这样无需修改 `ManagerBasedRlEnv.step()` 或 RSL-RL 的 `VecEnv` 接口。

### 10.4 reward 时刻对齐

RSL-RL 的流程是：

```text
obs_t -> action_t -> env.step
      -> obs_t+1, reward_t, extras
      -> algorithm.process_env_step
```

`extras["torque_costs"]` 必须描述 `action_t` 执行后的 requested/applied torque，
并且必须在该环境可能发生的自动 reset 之前捕获。自定义 PPO 应在
`process_env_step()` 中先构造：

```text
reward_aug_t = reward_t - lambda * cost_t
```

再调用父类存储 transition。

## 11. ActuatorStressTracker

### 11.1 推荐实现

新增：

```text
src/mjlab/tasks/velocity/config/rlboy/actuator_stress.py
```

实现：

```text
ActuatorStressTracker
```

由于 `EventManager` 在 `ObservationManager` 之前构造，而 `MetricsManager` 在
`ObservationManager` 之后构造，建议将“状态所有权”和“更新触发器”分开：

```text
EventManager:
  events["actuator_stress_state"]
  mode = "startup"
  func = ActuatorStressTracker

MetricsManager:
  metrics["actuator_stress_update"]
  func = update_actuator_stress
```

`ActuatorStressTracker` 是状态容器。其 startup `__call__()` 只完成校验或 no-op；
真正的逐步更新由 `update_actuator_stress` 在 `MetricsManager.compute()` 中调用：

```python
tracker = env.event_manager.get_term_cfg(
  "actuator_stress_state"
).func
value = tracker.update(env)
env.extras["torque_costs"] = tracker.training_costs().clone()
return value
```

当前调用顺序是：

```text
physics substeps
-> termination
-> reward
-> MetricsManager.compute()
-> automatic reset
-> step events
-> observations
```

因此 tracker 能捕获终止步力矩。partial reset 时由
`EventManager.reset(env_ids)` 调用 tracker 的 `reset(env_ids)` 清理对应状态。
不要把逐步更新放进 step event，否则它会在自动 reset 之后运行，终止步 cost
和 EMA 都会错位。

phase mask 不应只读取上一周期的 `recovery_assist.fallen_detected`。tracker 应
使用当前 posture estimator 重新计算 `needs_recovery`，再与
`recovery_assist.assist_active` 合并。

内部状态：

```text
requested_torque       [B, 20]
applied_torque         [B, 20]
gap_ratio              [B, 20]
previous_applied       [B, 20]
delta_torque_ratio     [B, 20]
stress_short           [B, 20]
stress_long            [B, 20]
walking_mask           [B]
recovery_mask          [B]
costs                  [B, K]
```

公开只读方法：

```python
def actor_torque_state(self) -> torch.Tensor: ...
def clip_gap_ratio(self) -> torch.Tensor: ...
def training_costs(self) -> torch.Tensor: ...
def metrics(self) -> dict[str, torch.Tensor]: ...
```

ObservationTermCfg 和 MetricsTermCfg 只通过
`EventManager.get_term_cfg("actuator_stress_state")` 查询同一个 tracker，不得
各自维护独立 EMA，否则不同 term 的状态会因调用次数不同而漂移。

`update_actuator_stress` 作为 metric term 仍需返回 `[B]`，建议返回：

```python
tracker.stress_long.amax(dim=1)
```

用于 episode 级 actuator-stress 日志；完整的 `[B, K]` cost 另行写入 extras。

### 11.2 更新时刻

当前环境每个 policy step 包含四个 physics substeps。首版 tracker 在四个 substep
完成后，以最新值更新一次。

论文中必须说明成本按 50 Hz 控制周期采样。若测试发现 substep 尖峰明显高于
控制周期末值，再实现：

```text
substep_requested_rms
substep_applied_rms
substep_peak_max
```

不要在没有数据证明前修改环境主循环。

### 11.3 partial reset

`reset(env_ids)` 只清理对应环境：

```text
stress_short[env_ids] = 0
stress_long[env_ids] = 0
previous_applied[env_ids] = 0
```

不得影响其他并行环境。

## 12. 配置扩展

### 12.1 模型配置

在 `RslRlModelCfg` 增加：

```python
dynamics_history_cfg: dict[str, Any] | None = None
```

建议配置：

```python
{
  "history_group": "actor_history",
  "command_group": "actor_command",
  "torque_state_group": "actor_torque_state",
  "history_length": 10,
  "frame_dim": 126,
  "command_dim": 3,
  "torque_state_dim": 40,
  "token_dim": 128,
  "token_hidden_dim": 256,
  "num_heads": 4,
  "ffn_dim": 256,
  "num_layers": 1,
  "dropout": 0.0,
  "pooling": "latest",
}
```

### 12.2 约束算法配置

建议新增 dataclass：

```text
RslRlRecoveryConstrainedPpoAlgorithmCfg
```

字段：

```python
class_name: str = (
  "mjlab.rl.constrained_ppo:RecoveryAwareConstrainedPPO"
)
cost_key: str = "torque_costs"
cost_names: tuple[str, ...] = (
  "walk_feasibility",
  "recovery_feasibility",
  "stress",
  "walk_smoothness",
)
budgets: dict[str, float] = ...
dual_lr: float = 1.0e-2
lambda_init: float = 0.0
lambda_max: float = 10.0
warmup_iterations: int = 200
dual_ramp_iterations: int = 200
```

不要给标准 PPO 传入这些额外参数。

### 12.3 新 runner 配置

新增：

```text
rlboy_raab_dyn_history_ppo_runner_cfg()
```

核心：

```python
cfg.actor.class_name = (
  "mjlab.rl.dynamics_history_model:DynamicsHistoryModel"
)
cfg.algorithm = RslRlRecoveryConstrainedPpoAlgorithmCfg(...)
cfg.obs_groups = {
  "actor": (
    "actor_history",
    "actor_command",
    "actor_torque_state",
  ),
  "critic": ("critic",),
}
cfg.experiment_name = "rlboy_velocity_raab_dyn_history"
```

## 13. 建议修改文件

### 13.1 必须新增

| 文件 | 作用 |
|---|---|
| `src/mjlab/tasks/velocity/config/rlboy/actuator_stress.py` | 统一执行器状态、EMA、cost 和 metric |
| `src/mjlab/rl/constrained_ppo.py` | 阶段条件 Primal-Dual PPO |
| `tests/test_rlboy_actuator_stress.py` | tracker、EMA、reset、cost 测试 |
| `tests/test_recovery_constrained_ppo.py` | augmented reward、dual update、save/load |

如果原 DynHist 尚未实现，还需新增：

| 文件 | 作用 |
|---|---|
| `src/mjlab/rl/dynamics_history_model.py` | 多 group history actor |
| `tests/test_dynamics_history_model.py` | 模型与导出测试 |

### 13.2 必须修改

| 文件 | 修改 |
|---|---|
| `src/mjlab/rl/config.py` | 增加模型和约束算法配置 |
| `src/mjlab/rl/runner.py` | 处理可选模型字段，保存算法扩展状态 |
| `src/mjlab/tasks/velocity/config/rlboy/env_cfgs.py` | 新 observation、tracker event、updater metric、cost 和 env cfg |
| `src/mjlab/tasks/velocity/config/rlboy/rl_cfg.py` | 新 runner cfg |
| `src/mjlab/tasks/velocity/config/rlboy/__init__.py` | 注册新任务 |
| `src/mjlab/rl/exporter_utils.py` | 增加历史、EMA 和力矩限制 metadata |
| RL_BOY velocity 部署脚本 | 维护 history 和双 EMA |

### 13.3 不建议修改

第一版保持不变：

- 原 flat/GRU 任务；
- 恢复辅助力状态机；
- 恢复初始状态分布；
- 速度命令课程；
- 动作空间和 action scale；
- DC motor 峰值 torque-speed envelope；
- critic 结构；
- 原 checkpoint 加载逻辑。

## 14. 指标体系

### 14.1 每关节/电机组指标

至少统计：

```text
requested_peak_ratio mean/p95/p99/max
applied_peak_ratio mean/p95/p99/max
applied_continuous_ratio RMS
requested > peak fraction
requested-applied clip fraction
clip gap RMS
stress_short mean/max
stress_long mean/max
stress violation fraction
torque-rate RMS/p99
positive mechanical power
torque-squared copper-loss proxy
```

分别聚合：

```text
arms
hips
knees
ankles
waist
all actuators
```

只报告全关节平均值会掩盖单个关键电机过载。

### 14.2 按任务阶段统计

必须分别报告：

- 普通初始状态行走；
- 倒地初始状态；
- 运行中被击倒；
- active recovery；
- 起身成功后 2 秒；
- 稳定行走阶段。

### 14.3 控制能力指标

- linear/angular velocity tracking error；
- normal survival rate；
- knockdown recovery success；
- zero-assist get-up success；
- recovery time；
- Task Resumption Time；
- recovery 后固定窗口 tracking error；
- action rate 和 action acceleration；
- foot slip 和姿态稳定性。

### 14.4 能效指标

若机器人质量为 `m`、重力为 `g`、水平距离为 `d`：

```text
CoT = integral(power dt) / (m * g * d)
```

近零速度和起身 episode 不适合使用 CoT，应分别报告：

```text
get-up energy per successful recovery
standing energy per second
walking Cost of Transport
```

## 15. 实验与消融

### 15.1 主对照

| 编号 | Actor | Torque state | 优化方式 |
|---|---|---|---|
| A | 当前 MLP | 无 | 当前 penalty |
| B | 当前 MLP | 无 | 改进固定 penalty |
| C | 10 帧扁平 MLP | 双 EMA | 改进固定 penalty |
| D | DynHist | 无 | 改进固定 penalty |
| E | DynHist | 双 EMA | 改进固定 penalty |
| F | DynHist | 双 EMA | 阶段条件 Primal-Dual |
| G | GRU | 双 EMA | 阶段条件 Primal-Dual |

F 是首版完整方法。

### 15.2 关键消融

1. 无 requested torque；
2. 无 applied torque；
3. 无 explicit gap；
4. 只有 0.2 秒历史，无 EMA；
5. 只有单个 2 秒 EMA；
6. 双 EMA；
7. 静态统一预算；
8. recovery/walking 阶段预算；
9. 固定 reward weight；
10. adaptive dual multiplier；
11. max pooling；
12. latest-token pooling。

### 15.3 OOD 测试

- payload 超出训练范围；
- friction 变化；
- PD gain 变化；
- motor strength 下降；
- torque-speed curve 参数误差；
- control delay；
- torque measurement noise/bias；
- requested/applied torque 时刻错位；
- 单个关节降额；
- 连续多次被击倒。

## 16. 论文完整版的辅助预测

在首版 F 稳定后，增加：

```text
input:
  dynamics embedding
  current normalized action

targets at next control step:
  applied torque ratio
  clip gap ratio
  saturation binary label
```

损失：

```text
L_aux =
  beta_applied * MSE(pred_applied, target_applied)
  + beta_gap * MSE(pred_gap, target_gap)
  + beta_sat * BCE(pred_sat, target_sat)
```

推荐从：

```text
beta_applied = 0.1
beta_gap = 0.1
beta_sat = 0.01
```

开始，并检查 auxiliary gradient 不应显著大于 PPO actor gradient。

该扩展需要自定义 rollout storage 保存 next-step target，工程复杂度明显高于首版。
若 F 已经产生清晰结果，可以将其作为额外消融，而不是强行加入。

## 17. 分阶段实施

### M0：冻结基线和预算标定

- 固定 MLP、GRU checkpoint 和 seeds；
- 增加只读 metric，不修改 reward；
- 收集各阶段 torque distribution；
- 确定四个 constraint budget。

完成标准：

- 能复现原成功率和 tracking；
- 可获得分阶段、分电机组的力矩报告；
- budget 不再依赖主观猜测。

### M1：ActuatorStressTracker

- 实现 requested/applied/gap；
- 实现 torque rate；
- 实现 0.5/2.0 秒 EMA；
- 完成 full/partial reset 测试；
- 输出 actor state、cost 和 metric。

完成标准：

- 人工构造 torque 序列的 EMA 与解析结果一致；
- partial reset 不影响其他环境；
- 无重复更新和一拍错位。

### M2：固定 penalty 基线

- 增加 requested、gap、stress、torque-rate reward；
- 使用固定权重短训；
- 校验策略确实能降低对应 cost；
- 确认起身没有因成本定义错误而崩溃。

完成标准：

- 所有 cost 对策略变化方向正确；
- 无 NaN/Inf；
- 形成论文的 reward-shaping baseline。

### M3：RAAB-DynHist actor

- 实现 `[B, 10, 126]` history；
- 接入 `[B, 40]` torque state；
- 完成 normalization、checkpoint、ONNX/JIT；
- 与 10 帧扁平 MLP 对比。

完成标准：

- PPO rollout/update 正常；
- 模型满足 50 Hz 部署；
- observation parity 通过。

### M4：阶段条件约束 PPO

- 实现 reset 前 `extras["torque_costs"]` 数据通道；
- 实现 augmented reward；
- 实现 dual update；
- 保存/恢复 lambda；
- multi-GPU 同步 rollout cost 和 lambda。

完成标准：

- lambda 在超预算时上升、低于预算时下降；
- resume 后 lambda 完全一致；
- 多 GPU 各 rank 使用相同 lambda；
- 至少三个 seeds 满足预算。

### M5：完整实验

- 完成主对照与必要消融；
- 绘制性能—力矩 Pareto；
- 完成 OOD 和噪声测试；
- 报告吞吐、显存、参数量和推理延迟。

完成标准：

- 能区分收益来自 cost、慢状态、历史结构还是自适应约束；
- 主要结论具有统计一致性。

### M6：辅助预测

仅在 M5 结论稳定后实施 next-step actuator prediction。

## 18. 测试要求

### 18.1 Tracker 测试

- requested torque actuator 顺序正确；
- applied torque actuator 顺序正确；
- gap 归一化正确；
- EMA alpha 与 dt/时间常数一致；
- constant continuous torque 最终收敛到 1；
- zero torque 下 EMA 单调衰减；
- torque rate 使用上一控制周期；
- full reset 和 partial reset；
- phase mask 切换；
- cost shape、dtype 和 device。

### 18.2 算法测试

- cost 只通过 extras 传递，不进入 actor/critic；
- augmented reward 数值正确；
- 空 phase mask 不更新 lambda；
- 超预算时 lambda 上升；
- 低于预算时 lambda 不小于零；
- warmup 和 ramp 正确；
- lambda max 生效；
- save/load 一致；
- standard PPO task 不受影响；
- multi-GPU cost 使用 all-reduce 后的全局均值。

### 18.3 模型测试

- 三输入 forward；
- normalization 独立；
- history shape 严格检查；
- torque state shape 严格检查；
- stochastic/deterministic action；
- backward 梯度有限；
- ONNX/JIT 与 PyTorch 一致。

### 18.4 集成测试

- CPU 小环境 rollout；
- 一次 PPO update；
- 短训练无 NaN；
- 原 flat 和 GRU smoke test；
- checkpoint resume；
- play；
- export；
- 部署 observation parity。

## 19. 风险与处理

| 风险 | 表现 | 处理 |
|---|---|---|
| 约束过强 | 起身成功率骤降 | recovery 独立预算、warmup、lambda 上限 |
| 约束过弱 | torque 指标不变 | 按 baseline 分布标定预算，不手调小权重 |
| EMA 不能代表真实温度 | sim 指标好但实机过热 | 明确称 I²t proxy；后续标定热模型 |
| requested torque 依赖仿真实现 | 实机协议不一致 | 从实机低层 PD 限幅前命令构造 |
| applied torque 噪声大 | gap 输入抖动 | 加噪声、偏置、延迟 DR 和滤波 |
| phase mask 泄露 | 部署依赖仿真状态 | phase 仅用于 cost，不进入 actor |
| cost 被样本比例稀释 | recovery 约束不起作用 | 按 phase 条件均值独立归一化 |
| Transformer 收益来自参数量 | 学术归因错误 | 参数匹配 MLP/GRU 和 flat-10 对照 |
| 只优化平均值 | 单电机仍过载 | 报告 group/joint p99、max 和 violation time |
| substep 峰值漏采样 | 控制周期指标过于乐观 | profile 后加入 substep RMS/max |
| 多 GPU lambda 不一致 | 训练结果异常 | cost all-reduce，rank 0 更新并广播 |

## 20. 研究验收标准

### 20.1 最低能力保持

相对原始 MLP：

```text
正常速度跟踪误差恶化 <= 3%
零辅助起身成功率下降 <= 3 个百分点
平均 recovery time 恶化 <= 5%
Task Resumption Time 恶化 <= 5%
```

### 20.2 力矩改善目标

建议目标：

```text
walking requested peak p99       降低 >= 15%
requested > peak fraction        降低 >= 30%
requested-applied clip fraction  降低 >= 30%
continuous stress violation time 降低 >= 30%
walking torque-rate RMS          降低 >= 15%
walking energy/CoT               降低 >= 10%
```

这些是项目验收目标，不是实施前能够保证的结果。最终论文应报告置信区间和至少
三个 seeds，而不是只报告最佳 run。

### 20.3 工程验收

- 原任务与 checkpoint 无变化；
- 新任务可 train、resume、play、ONNX/JIT export；
- inference 显著低于 20 ms；
- tracker 和 history partial reset 正确；
- 部署端与环境逐元素一致；
- torque limits、EMA 参数、term 顺序写入 metadata；
- 输入或 metadata 不匹配时明确拒绝加载。

## 21. 推荐论文主张

若完整实验支持，推荐将贡献总结为：

1. 提出面向统一倒地恢复和速度跟踪的阶段条件执行器预算，使策略能够区分
   “恢复所需的合理短时峰值”和“稳定行走中的无效持续过载”。
2. 提出 requested-applied actuator discrepancy history，将控制意图、实际执行和
   torque-speed 限幅之间的差异用于在线可实现性表征。
3. 联合短时神经动力学历史与长时解析式执行器负载，在不引入递归部署状态的
   情况下改善持续力矩管理。
4. 在 normal walking、fallen initialization、runtime knockdown 和 post-recovery
   resumption 上系统展示任务性能与执行器安全之间的 Pareto 改善。

不建议把主要贡献写成：

- 使用了 Transformer；
- 使用了 torque reward；
- 使用了 EMA；
- 使用了 PPO-Lagrangian。

论文价值来自这些组件围绕起身—行走冲突目标形成的统一问题定义、方法和实证。

## 22. 参考工作

- Kumar et al., *RMA: Rapid Motor Adaptation for Legged Robots*, 2021:  
  <https://arxiv.org/abs/2107.04034>
- Kumar et al., *Adapting Rapid Motor Adaptation for Bipedal Robots*, 2022:  
  <https://arxiv.org/abs/2205.15299>
- Shin et al., *Actuator-Constrained Reinforcement Learning for High-Speed
  Quadrupedal Locomotion*, 2023:  
  <https://arxiv.org/abs/2312.17507>
- Ma et al., *Robust and Generalized Humanoid Motion Tracking*, 2026:  
  <https://arxiv.org/abs/2601.23080>
- Huang et al., *ECO: Energy-Constrained Optimization with Reinforcement
  Learning for Humanoid Walking*, 2026:  
  <https://arxiv.org/abs/2602.06445>
- Qian et al., *Learning Thermal-Aware Locomotion Policies for an
  Electrically-Actuated Quadruped Robot*, 2026:  
  <https://arxiv.org/abs/2603.01631>
- *VPIES: Variational Privileged Information Encoder as Scaffold for Legged
  Locomotion Learning*, 2026:  
  <https://ieeexplore.ieee.org/document/11403985>

## 23. 最终实施建议

项目应先实现：

```text
ActuatorStressTracker
+ requested/applied gap
+ 双 EMA
+ 分阶段 metrics
```

然后实现：

```text
改进固定 penalty baseline
```

最后实现完整方法：

```text
RAAB-DynHist
+ recovery/walking 条件预算
+ Primal-Dual PPO
```

这个顺序可以保证每一步都能独立验证，避免网络、奖励、约束和执行器状态同时改变
后无法定位收益来源。最小具备论文价值的版本应至少包含：

```text
执行器差值历史编码
+ 双时间尺度力矩状态
+ 阶段条件自适应约束
```
