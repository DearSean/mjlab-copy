# Velocity 无机器人专属动作文件起身系统

## 1. 最终目标

Velocity 策略应使用同一个 actor 完成速度追踪、跌倒检测、自主起身和
恢复行走。核心训练链路不读取人工制作或自动重定向的机器人动作轨迹，
也不逐帧模仿人体或机器人参考动作。

系统的核心数据流是：

```text
LaFAN BVH ──► 通用人体恢复表示 ──► MLD 动作增广 ──► SMP 语义先验
                                                        │
机器人实时状态 ──► 同一语义表示 ────────────────────────┤
                                                        ▼
物理原生倒地 reset ─────────────────────────────► Velocity PPO
                                                        │
                                                        ▼
                                    reference-free recovery actor
```

MLD 只离线生成人体语义动作，SMP 只在训练期评价短时动作是否符合恢复
运动分布，PPO 才是实际机器人控制器。RL_BOY 与 G1 复用同一套数据、
先验和训练代码，但分别训练策略。

## 2. 核心模块

### 2.1 LaFAN 数据与通用恢复表示

**功能**

- 解析全部 LaFAN BVH，保留骨架、局部旋转、根运动、帧率和来源信息。
- 提取倒地、地面运动、受推跌倒和起身片段，标注倒地类别、接触、恢复
  进度、支撑完成帧以及静态站立/直接运动两类成功终点。只完成有价值
  支撑过程的片段保留为 `support_only`，但不作为完整恢复监督。
- 输出固定 93 维的机器人无关语义状态：root 高度 1 维、躯干 6D 朝向
  6 维、root 局部线/角速度 6 维、9 个语义点的相对位置和速度 54 维、
  8 个接触部位的离地高度 8 维、接触标志 8 维、语义点 mask 9 维以及
  恢复进度 1 维。
- 按 recording 分组划分训练、验证和测试集，禁止同一动作录制的不同
  subject 或相邻窗口跨集合泄漏。

**验收标准**

- 工作区 77/77 个 BVH 均能转换，且没有 NaN/Inf。
- FK 位置重建误差不超过 `1e-5 m`，Y-up 到 Z-up 转换保持右手系。
- 全局水平平移、yaw 和整体尺度变化后，规范化语义特征误差不超过
  `1e-5`。
- 每个片段都能追溯到来源文件、recording、subject 和帧区间。
- 复核队列、人工标注、指标与 reviewed manifest 集中存放，原始 BVH 和
  自动 manifest 保持不可变；全部候选必须被接受或拒绝。
- 200 帧人工复核中，接触和恢复阶段标签的 macro-F1 不低于 0.90。

### 2.2 MLD 人体恢复动作增广

**功能**

- 在完整 LaFAN 上预训练轻量 MLD VAE，在恢复片段上微调。
- 使用“初始 1--4 帧、目标终点类型、倒地类别和动作长度”作为生成条件；
  终点类型同时支持稳定站立和起身后直接进入行走/跑动。
- 生成 4--8 秒的人体恢复动作并转换成与真实数据相同的 93D 序列。
- MLD 不输出 robot qpos，不进入 PPO 在线推理。

**验收标准**

- held-out 动作重建 MPJPE 不超过 50 mm，root height MAE 不超过
  20 mm，接触 F1 不低于 0.85。
- 至少 90% 的合格生成动作满足初态连续、骨长漂移不超过 1%，并达到
  指定的静态站立终点或安全的动态运动接管终点。
- 仰卧、俯卧、左右侧卧均可条件生成，不逐帧复制训练片段。
- 输出不包含机器人名称、关节顺序或机器人动作数组。

### 2.3 机器人语义投影

**功能**

- `RecoveryRobotSpec` 只描述 root、躯干、双肘、双手、双膝、双脚、
  接触部位和归一化尺度，不描述动作。
- 将 RL_BOY/G1 的实时仿真状态投影到与 LaFAN 完全相同的 93D 表示。
- 每个新机器人只增加声明式 body/site 映射，不增加动作文件或关键姿态。

**验收标准**

- RL_BOY 和 G1 使用同一投影算法，算法内没有机器人名称分支。
- 环境初始化时验证全部语义映射，缺失 body/site 时立即失败。
- 投影结果有限，历史窗口在 reset 后不跨 episode 污染，并对全局水平
  平移和 yaw 保持不变。
- 两种机器人加载完全相同的 SMP schema、normalizer 和 checkpoint。

### 2.4 物理原生倒地状态与难例课程

**功能**

- 通过随机 root 朝向、限位内关节扰动、随机推力、低增益 settling 和
  策略历史失败状态直接在 MuJoCo 中产生倒地初态。
- 对状态执行关节限位、自碰撞、地面穿透、速度和接触质量过滤。
- 按倒地类别、恢复进度和历史失败率自适应采样，同时保留均匀探索。
- 临时状态池只能作为加速缓存；删除后必须能够从 MJCF 和随机种子重建。

**验收标准**

- 每台机器人可在不读取动作文件的情况下生成至少 10,000 个有效状态，
  仰卧、俯卧、左右侧卧各不少于 2,000 个。
- 有效状态比例不低于 90%，没有 NaN、硬限位违规或超过 2 mm 的持续
  穿透。
- 高失败阶段的采样概率会增加，同时至少保留 10% 均匀采样概率。
- checkpoint 恢复后课程分布和 RNG 连续，评测不污染训练统计。

### 2.5 MimicKit SMP 语义动作先验

**功能**

- 借鉴 TinyMDM、SDS 和 SMP，但输入改为 `[batch, 16, 93]` 的通用
  语义窗口，而不是某台机器人的全部关节旋转。
- 使用同一个 prior 对 RL_BOY 和 G1 的恢复动作评分，不输出目标关节角。
- 将超过真实恢复动作分布范围的 score loss 定义为风格成本：

  ```text
  c_style = clip(relu((L_smp - P90_real) / (P90_real - P50_real)), 0, 3)
  ```

- PPO 奖励使用恢复阶段门控、自适应拉格朗日权重和风格改善项：

  ```text
  r = r_task - lambda * recovery_gate * c_style
      + eta * recovery_gate * (c_style_prev - c_style)
  ```

- reset 后等待完整历史窗口；支撑完成后逐渐降低 SMP 权重，进入正常
  行走后关闭 SMP，避免人体跑动风格约束 Velocity 控制器。

**验收标准**

- held-out 真实窗口与时间打乱、接触破坏窗口的区分 AUC 不低于 0.80。
- RL_BOY 和 G1 使用相同 prior、normalizer 和 schema 哈希，不进行
  机器人专属微调。
- 奖励始终有限，SMP 对 advantage 波动的贡献限制在任务奖励的
  15%--25%，PPO 吞吐下降不超过 25%。
- actor observation 不包含 score、diffusion timestep、phase 或 latent。

### 2.6 Velocity 自主起身与速度追踪联合策略

**功能**

- 默认 Recovery 任务不再配置 CSV 或机器人动作状态源。
- 延续恢复进度、稳定站立、能耗、力矩和速度追踪奖励；SMP 只约束动作
  风格，不能替代站立目标。
- 恢复期间平滑关闭速度追踪要求，稳定站立后平滑恢复原速度命令。
- actor 只观察部署可用的本体感知和速度命令；critic 可使用恢复进度、
  接触和 SMP 风格成本。
- Rough terrain 使用机器人脚下的局部地面高度。

**验收标准**

- 默认 RL_BOY/G1 Recovery 配置中不存在 CSV、重定向轨迹或动作缓存
  路径。
- 零辅助力评测中，两种机器人对仰卧、俯卧、左右侧卧和随机跌倒分别
  达到 5 秒内至少 90% 的恢复成功率。
- 零速度命令下，成功要求高度、直立度和低运动速度持续至少 0.5 秒；
  非零速度命令下，也允许无非足部持续接触、没有再次倒地且逐渐进入
  命令速度追踪的动态恢复成功。
- 恢复确认后 3 秒内的速度追踪 RMSE 不超过纯 Velocity 基线的 110%。
- 导出的 actor 在没有 LaFAN、MLD、SMP、Holosoma、DSMS 和状态池时
  能够独立推理。

### 2.7 评测、消融与可复现性

**功能**

- 建立数据质量、先验判别能力、物理 reset 质量和闭环策略四层评测。
- 固定核心消融：现有 RL_BOY CSV、物理 reset 无 SMP、真实 LaFAN
  SMP、LaFAN+MLD SMP，以及完整系统去除难例采样。
- 报告成功率、起身时间、恢复后速度追踪、峰值力矩、能耗、碰撞、接触
  滑移和训练吞吐。
- 平地训练，rough terrain 只做零样本泛化评测。

**验收标准**

- 使用 5 个训练种子；每个机器人、每种倒地类别、每个种子至少评测
  500 个 episode，并报告均值、标准差和 95% 置信区间。
- Flat terrain 满足逐类别 90% 成功率；rough terrain 零样本平均成功率
  目标不低于 75%。
- 相同 checkpoint、配置和随机种子得到一致结果。
- 自动输出机器可读 JSON/CSV，并记录代码、模型、数据和配置哈希。

## 3. 可选对照：Holosoma 与 DSMS

Holosoma interaction-mesh SQP 可以作为自动运动学重定向基线，DSMS 可以
作为动力学可行的机器人参考上界。它们生成的 robot-specific 轨迹只能用于
消融、reset 对照或 teacher 上界，不能成为核心训练的必要输入，也不能提供
actor 逐帧参考。

可选对照必须满足无硬限位违规、持续穿透不超过 2 mm、语义关键点误差不
超过机器人站立高度的 5%，并且 DSMS 归一化 shooting defect 不超过
`1e-4`。删除全部重定向结果后，核心训练和推理仍必须可运行。

## 4. 方法边界

- 核心方法不执行人体动作到机器人轨迹的重定向，不属于 imitation learning。
- “机器人无关”针对动作数据和动作先验；机器人仍需要语义 body 映射，且
  每种机器人分别训练策略。
- MLD 是离线人体动作增广器，SMP 是训练期动作分布约束，PPO 是控制器。
- 使用 Holosoma Apache-2.0 代码时必须保留 NOTICE；LaFAN 数据、衍生
  数据和模型权重在发布前单独核验许可。
